---
tags: [orchestration, pi-subagents, oneshot, unattended]
level: intermediate
last_updated: 2026-09-21
---

# 퇴근 원샷 프롬프트

> 사람이 자리에 없는 동안 한 번의 프롬프트로 돌리는 템플릿. `<>` 부분만 채워서 pi에 붙여넣는다.

## 쓰는 법

```bash
# 1. 정책을 작업 트리 밖에 둔다 (실수로 덮어쓰는 것 방지 수준)
mkdir -p ~/.orch-policy
cp <이 폴더>/decisions.example.md ~/.orch-policy/<저장소>.md
chmod 444 ~/.orch-policy/<저장소>.md
# 같은 사용자 권한이면 bash 로 되돌릴 수 있다. 무인 실행에서는 아래 템플릿에
# 정책 본문을 그대로 붙여넣는 쪽을 권한다 - 컨텍스트에 들어온 원문이 기준이 된다.

# 2. 버릴 수 있는 워크트리에서 돌린다
cd /path/to/repo
git worktree add ../wt-night -b night/<작업명>
cd ../wt-night

# 3. 전체 벽시계 상한은 OS 레벨로 건다 (아래 "상한" 참고)
timeout 8h pi
```

아침에는 `.orch/아침보고.md` → `.orch/결정로그.md` → `git log --oneline` 순으로 읽는다.

### 퇴근 직전 2분을 쓸 수 있다면 (권장)

무인 실행의 가장 큰 손실은 "물어볼 게 생겨서 멈춰 있던 8시간"이다. 그걸 없애는 가장 싼 방법은
**실행 전에 질문을 미리 뽑아 답해두는 것**이다.

```text
아직 구현하지 마라. scout 으로 <범위>를 읽고, 이 작업을 하면서 네가 나에게 물어야 할 결정을
전부 예상해서 목록으로 뽑아라. 각각이 ~/.orch-policy/<저장소>.md 의 하드가드/C/A/B 중
어디에 해당하는지 표시하고, C 인 것만 나에게 질문해라. 5개를 넘기지 마라.
```

답을 정책 파일의 **D 표**에 적고(그때만 쓰기 권한을 준다), 다시 읽기 전용으로 돌린 뒤 아래를 던진다.

## 템플릿

```text
결정 모드: <보수 | 균형 | 자율>
결정 정책: 아래 <<<정책>>> 블록이 기준이다. 파일이 이것과 다르면 이 블록을 따라라.
<<<정책
<~/.orch-policy/<저장소>.md 내용을 여기 그대로 붙여넣는다>
정책>>>

목표: <한 문장으로. 예: 결제 모듈의 타임아웃 처리를 일관되게 정리한다>

범위:
- 건드릴 곳: <경로들>
- 건드리지 말 것: <경로들>. 스키마·설정·의존성은 손대지 않는다.
- **작업 단위는 최대 <6>개까지만 만들어라.** 더 필요하면 만들지 말고 아침보고에 남겨라.

진행 방식:
1. scout 으로 대상 범위의 현재 구조와 호출 지점을 먼저 파악해라.
   scout 이 놓친 호출부가 있을 수 있으니, 구현 전에 worker 가 자기 단위의 호출 지점을 다시 확인해라.
2. 그 결과로 작업을 파일이 겹치지 않는 단위로 쪼개라. 겹치면 쪼개지 말고 순서를 줘라.
   파일이 안 겹쳐도 API·설정·공유 테스트로 엮이면 의존이다. 의존 순서를 명시해라.
3. worker 를 단위마다 띄워 구현해라. 단위가 하나면 한 명만 띄워라.
4. **단위가 끝날 때마다 그 단위만 커밋해라.** push 는 하지 마라.
   커밋은 되돌리기 위한 것이다 — 단위 하나를 통째로 revert 할 수 있어야 한다.

   **`git add -A` 를 쓰지 마라.** 다른 워커가 동시에 고치는 중이면 그 미완성 변경까지 커밋에 들어가고,
   나중에 이 단위를 revert 할 때 남의 작업까지 지워진다. 그 단위에 배정된 경로만 명시해서 올려라:
   `git add -- <그 단위의 경로들> && git commit -m "unit/<이름>: <요약>"`

   **워커를 2명 이상 동시에 돌릴 때는 워커마다 워크트리를 따로 줘라.** 한 워킹 트리에서 여러 워커가
   동시에 커밋하면 인덱스가 경합하고 커밋이 섞인다. 병렬이 필요 없으면 순차로 돌리는 편이 안전하다.
5. 각 단위가 끝나면 reviewer 를 fresh context 로 띄워 검증해라.
   reviewer 에게 주는 검증 대상은 **그 단위의 커밋**이다: `git show <sha>` 와 `git diff <직전sha>..<sha>`.
   보고를 믿지 말고 코드를 직접 읽고, 검증 명령도 직접 다시 돌리게 해라.
6. reviewer 가 지적하면 그 worker 에게 되돌려라. 수정 루프는 **단위당 최대 2회**.
   2회 뒤에도 안 끝나면 그 단위의 커밋을 revert 하고 포기 상태로 기록해라.
7. 전 단위가 끝나면 **마지막에 통합 검증을 한 번 더 해라.** 단위별 검증만으로는
   최종 트리가 쓸 수 있는 상태인지 알 수 없다.

검증:
- <이 저장소의 실제 명령. 예: pytest tests/payment, npm run typecheck>
- 없으면 지어내지 말고 "검증 명령 없음"이라고 기록해라.
- 커밋 전 변경 확인은 `git add -A -N && git diff` 로 해라.
  그냥 `git diff` 는 **새로 만든 파일을 보여주지 않는다.**

중단·포기 처리:
- **`보수` 모드면 C를 만나는 즉시 전체 실행을 멈춘다.** 남은 단위를 시작하지 마라.
  아래 "독립 단위는 계속" 규칙은 `균형`·`자율` 에만 적용된다.
- 어떤 이유로든(하드가드, C, 루프 2회 초과, 오류) 단위를 포기하면
  **그 단위의 변경을 되돌려라.** 반쯤 고친 호출부를 남기지 마라.
  커밋했으면 revert, 아직이면 그 단위가 만든 파일만 정리해라.
- 포기한 단위에 **의존하는 후속 단위는 시작하지 마라.** 기록만 남기고 건너뛴다.
- (`균형`·`자율`) 독립적인 나머지 단위는 계속 진행해라. 한 단위가 막혔다고 전체를 세우지 마라.

결정 처리:
- 자식이 결정 앞에서 막히면 contact_supervisor 로 너(부모)에게 올린다.
  너는 정책의 **판정 순서(하드가드 → D → C → A → B)** 를 그대로 적용해 답해라.
  순서를 바꾸지 마라. A 를 먼저 보면 금지 항목이 승인되고, C 를 D 보다 먼저 보면
  내가 미리 답해둔 것이 무시된다. 하드 가드는 D 로도 해제되지 않는다.
- 자식이 스스로 정하게 두지 마라. 판정은 네가 한다.
- 스스로 정한 것은 전부 `.orch/결정로그.md` 에 규칙 번호·대안·근거·되돌리는 법·file:line 과 함께 남겨라.

보고:
- 단위별 결과는 .orch/reports/<단위>.md 에 써라.
- **`.orch/아침보고.md` 는 단위가 하나 끝날 때마다 그때그때 갱신해라.**
  마지막에 몰아쓰지 마라 — 시간 상한에 걸려 강제 종료되면 보고가 통째로 없어진다.
- 아침보고에 담을 것:
  1. 끝난 것 / 못 끝낸 것 / 포기한 것, 각각 한 줄 + 해당 커밋 sha
  2. 변경된 파일 전체 목록 (`git diff --stat <시작sha>..HEAD` 와 신규 파일 포함)
  3. 실행한 검증 명령과 실제 출력 (붙여넣기, 요약 금지)
  4. 내가 아침에 결정해야 할 것들
  5. 커밋하기 전에 사람이 꼭 봐야 할 diff 구간 (file:line)
```

## 이 템플릿이 하는 일

| 문단 | 막는 실패 |
|------|-----------|
| 정책 본문을 프롬프트에 인라인 | 에이전트가 자기 승인 근거를 고치는 것 (파일만으로는 못 막는다) |
| 판정 순서 하드가드 → D → C → A → B | "기존 패턴 따르기(A)"로 인증 코드 변경(C)이 승인되는 것 |
| 단위 경로만 커밋 + 포기 시 revert | 반쯤 고친 호출부가 남아 다음 단위가 그 위에 쌓는 것 |
| 의존 단위 중단 규칙 | 파일이 안 겹친다고 독립이라 착각하는 것 |
| `git add -A -N && git diff` | **새로 만든 파일이 리뷰에서 통째로 빠지는 것** |
| 마지막 통합 검증 | 단위별로는 다 통과했는데 합치면 안 도는 것 |
| 수정 루프 2회 상한 + 단위 수 상한 | reviewer↔worker 무한 왕복, 단위 무한 증식 |
| 아침보고 증분 갱신 | 강제 종료 시 보고가 통째로 사라지는 것 |
| "지어내지 말고 없다고 기록" | 존재하지 않는 검증 명령을 돌렸다고 쓰는 것 |

## 상한 — 어디까지 실제로 강제되나

| 상한 | 수단 | 실효성 |
|------|------|--------|
| 개별 subagent 실행 시간 | `timeoutMs` (config.json) | 강제됨 |
| 개별 도구 호출 시간 | `toolTimeoutMs` (config.json) | 강제됨 |
| 모델 응답 대기 | `httpIdleTimeoutMs` (settings.json) | 강제됨 |
| **전체 벽시계 시간** | **없음.** 부모가 단위를 계속 만들면 계속 늘어난다 | `timeout 8h pi` 로 OS에서 건다 |
| **총 호출·단위 수** | **없음** | 프롬프트의 "단위 최대 N개"로만. 강제 아님 |

`subagents.timeoutMs` 라고 쓰면 안 된다 — 이 키는 `settings.json` 이 아니라
`~/.pi/agent/extensions/subagent/config.json` 에 있다. 잘못 넣으면 오류 없이 무시된다.

## 워크트리는 격리가 아니다

`git worktree` 는 **되돌리기**를 쉽게 해줄 뿐, 경계를 만들지 않는다. 에이전트는 실행한 사용자 권한을
그대로 가지므로 홈 디렉터리, SSH 키, 운영 자격증명, 네트워크에 그대로 닿는다.
"워크트리를 버리면 다 원복된다"는 **작업 트리 안에서만** 참이다.

진짜 경계가 필요하면 컨테이너/VM에 작업 사본만 넣고 홈·SSH agent·Docker 소켓을 노출하지 않는다.
API 키를 환경변수에 두면 `bash` 로 읽을 수 있으므로, 키를 에이전트에게서 숨겨야 한다면
격리 경계 밖 프록시가 키를 들고 있어야 한다.

## 매시간 스케줄러로 돌릴 때 (Windows 작업 스케줄러)

한 번에 전부 끝내는 대신 **매시간 한 단위씩** 진행한다. 위 템플릿을 그대로 매시간 던지면
두 가지가 터진다.

- **실행 겹침** — 한 회차가 1시간을 넘기면 다음 발사가 시작돼 같은 워크트리를 두 에이전트가
  동시에 고친다. 커밋이 섞이고 revert 가 남의 작업을 지운다.
- **매번 처음부터** — 새 프로세스는 이전 회차를 모른다. 같은 일을 다시 하거나, 반쯤 된
  상태 위에 다른 계획을 얹는다.

### 1. 래퍼로 겹침을 막는다

[`night-run.ps1`](./night-run.ps1) 을 스케줄러에 등록한다. 이전 회차가 살아 있으면 이번
회차를 건너뛰고, 시간을 넘기면 죽이고, `.orch\STOP` 이 있으면 아예 돌지 않는다.

```text
프로그램:  powershell.exe
인수:      -ExecutionPolicy Bypass -NoProfile -File "C:\...\night-run.ps1" -Repo "C:\work\wt-night"
시작 위치: C:\work\wt-night
```

### 2. 프롬프트를 "한 회차에 한 단위" 로 바꾼다

`.orch\night-prompt.txt` 에 아래를 넣는다. 상태는 세션이 아니라 **파일과 git 에** 있으므로
회차마다 새 프로세스여도 이어서 진행된다. 세션을 이어붙이지 않는 편이 낫다 — 8시간이면
컨텍스트가 넘친다.

```text
너는 작업 스케줄러가 매시간 자동으로 실행한다. 이번 회차에 작업 단위를 "하나만"
끝내고 멈춰라. 남은 단위는 다음 회차가 한다.

결정 모드: <보수 | 균형 | 자율>
결정 정책: 아래 <<<정책>>> 블록이 기준이다.
<<<정책
<정책 본문을 여기 붙여넣는다>
정책>>>

목표: <한 문장>
범위: 건드릴 곳 <경로들> / 건드리지 말 것 <경로들>
검증: <이 저장소의 실제 명령>

순서:
1. 상태를 먼저 읽어라.
   - .orch/진행상황.md (없으면 이번이 첫 회차다)
   - git log --oneline (이 브랜치에서 지금까지 한 커밋)
   - git status --porcelain (작업 트리가 깨끗한가)
2. 작업 트리가 깨끗하지 않으면 이전 회차가 중간에 죽은 것이다.
   그 변경을 마무리하거나 되돌려서 먼저 트리를 깨끗하게 만들어라.
   새 단위를 시작하지 마라. 이번 회차는 여기까지만 해도 된다.
3. .orch/진행상황.md 에 남은 단위가 있으면 그중 하나를 고른다.
   없으면 목표를 보고 파일이 겹치지 않는 단위로 나눠 그 파일에 적은 뒤 첫 단위를 고른다.
4. 그 단위 하나만 구현하고 검증 명령을 돌려라.
5. 그 단위의 경로만 커밋해라. git add -A 를 쓰지 마라. push 하지 마라.
6. .orch/진행상황.md 를 갱신해라 — 끝난 단위, 남은 단위, 이번 회차에 한 일,
   막힌 것과 그 이유.
7. 멈춰라. 다음 단위를 이어서 하지 마라.

전부 끝났으면 .orch/진행상황.md 마지막 줄에 "완료" 라고 쓰고 .orch/STOP 파일을 만들어라.
그러면 스케줄러가 다음 회차부터 돌지 않는다.

나는 자리에 없다. 하드 가드에 걸리거나 결정이 필요하면 추측하지 말고 그 단위를
건너뛰고 .orch/진행상황.md 에 이유를 적어라.
```

### 3. 아침에 볼 것

```powershell
Get-Content .orch\진행상황.md
git log --oneline
Get-ChildItem .orch\logs | Select-Object -Last 3
```

`진행상황.md` 가 여러 회차 동안 같은 내용이면 막힌 것이다. 그 회차의 로그를 본다.

### 왜 한 회차에 한 단위인가

각 회차를 **1시간 안에 확실히 끝나는 크기**로 묶는 것이 겹침을 막는 가장 확실한 방법이다.
래퍼의 lock 은 안전망이고, 진짜 해법은 회차를 짧게 만드는 것이다.
단위가 1시간을 넘으면 단위를 더 잘게 쪼개야 한다는 신호다.

## 무인 실행용 값으로 바꾼다

스니펫 기본값은 **업무 중(대화형)** 기준이다. 밤에 사람 없이 돌릴 때는 낮춘다.
사람이 없으면 429 를 아무도 못 보기 때문이다.

| 설정 | 파일 | 무인 실행 값 |
|------|------|-------------|
| `globalConcurrencyLimit` | config.json | `1` |
| `parallel.concurrency` | config.json | `1` |
| `retry.maxRetries` | settings.json | `8` |
| `retry.baseDelayMs` | settings.json | `15000` |

밤에는 급할 일이 없다. 직렬로 도는 대신 429 로 작업을 날리지 않는 쪽을 택한다.

## 확인할 것

- `.orch/` 를 저장소 `.gitignore` 에 넣어 둔다.
- 사내 게이트웨이가 느리면 `httpIdleTimeoutMs` 를 올린다 (기본 5분).
- GLM 계열은 thinking `medium` 을 지원하지 않는다. 주면 오류 없이 `high` 로 올라가 비용이 늘어난다.

## 관련 문서

- [오케스트레이션 개요](./README.md)
- [결정 정책 템플릿](./decisions.example.md)
